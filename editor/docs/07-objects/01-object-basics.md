# object basics

Objects group related data using key-value pairs. Values can be accessed with dot notation or bracket notation.

## Example
```js
const user = {
  name: "Jordan",
  role: "admin"
};

console.log(user.name);
console.log(user["role"]);
```
