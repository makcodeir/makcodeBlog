# destructuring

Destructuring extracts values from objects or arrays into variables. It makes code shorter when working with structured data.

## Example
```js
const user = {
  name: "Riley",
  email: "riley@example.com"
};

const { name, email } = user;
console.log(name, email);
```
