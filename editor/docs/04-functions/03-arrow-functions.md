# arrow functions

Arrow functions provide a compact syntax for writing functions. They are commonly used for callbacks and short transformations.

## Example
```js
const numbers = [1, 2, 3];
const doubled = numbers.map((number) => number * 2);

console.log(doubled); // [2, 4, 6]
```
