# function expressions

A function expression stores a function in a variable. This is useful when passing functions around or choosing a function dynamically.

## Example
```js
const multiply = function (a, b) {
  return a * b;
};

console.log(multiply(3, 5)); // 15
```
