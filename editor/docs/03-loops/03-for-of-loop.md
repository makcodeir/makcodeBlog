# for of loop

Use `for...of` to loop over iterable values such as arrays, strings, maps, and sets.

## Example
```js
const colors = ["red", "green", "blue"];

for (const color of colors) {
  console.log(color);
}
```
