# if else

Use `if`, `else if`, and `else` to run code based on conditions.

## Example
```js
const temperature = 18;

if (temperature > 25) {
  console.log("Hot");
} else if (temperature >= 15) {
  console.log("Mild");
} else {
  console.log("Cold");
}
```
