# string methods

String methods help inspect and transform text. Common methods include `trim`, `toLowerCase`, `includes`, and `replace`.

## Example
```js
const text = "  JavaScript Editor  ";

console.log(text.trim());
console.log(text.toLowerCase());
console.log(text.includes("Script"));
```
