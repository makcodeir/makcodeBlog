# array methods

Array methods help add, remove, find, and transform values. Common methods include `push`, `pop`, `includes`, and `join`.

## Example
```js
const tasks = ["write", "review"];

tasks.push("publish");
console.log(tasks.includes("review")); // true
console.log(tasks.join(", ")); // write, review, publish
```
