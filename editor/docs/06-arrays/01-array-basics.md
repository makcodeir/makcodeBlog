# array basics

Arrays store ordered lists of values. Each item has an index, starting at `0`.

## Example
```js
const fruits = ["apple", "banana", "orange"];

console.log(fruits[0]); // apple
console.log(fruits.length); // 3
```
